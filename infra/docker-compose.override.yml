# Local development overrides
# Usage: docker compose -f infra/docker-compose.yml -f infra/docker-compose.override.yml up

services:
  api:
    working_dir: /app/packages/llama-api
    command: bun run dev
    environment:
      - NODE_ENV=development

  bot:
    working_dir: /app/packages/llama-bot
    command: bun run dev
    environment:
      - NODE_ENV=development

  client:
    # Client uses nginx in production, for dev you might want to run it locally
    # or use a different override. Commented out for now.
    # working_dir: /app/packages/llama-client
    # command: bun run dev --host
    # ports:
    #   - "5173:5173"

