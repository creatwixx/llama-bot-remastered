# =============================================================================
# Llama Bot Remastered - Environment Variables
# =============================================================================
# 
# Copy this file to .env.local and fill in your actual values:
#   cp infra/.env.local.example infra/.env.local
#
# This file is for LOCAL DEVELOPMENT only.
# For production (Railway), set these variables in the Railway dashboard.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# PostgreSQL connection string from Railway
# For local development, connect directly to your Railway database:
#   1. Go to your Railway project dashboard
#   2. Click on your PostgreSQL service
#   3. Go to the "Variables" tab
#   4. Copy the DATABASE_URL value
#   5. Paste it here
#
# Example format:
#   postgresql://postgres:password@containers-us-west-xxx.railway.app:5432/railway
DATABASE_URL=postgresql://user:password@hostname.railway.app:5432/railway

# -----------------------------------------------------------------------------
# Discord Bot Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Your Discord bot token from https://discord.com/developers/applications
# Create a bot application and copy the token from the "Bot" section
DISCORD_TOKEN=your_discord_bot_token_here

# Your Discord Application ID (also called Client ID)
# Found in the "General Information" section of your Discord application
DISCORD_APP_ID=your_discord_app_id_here

# Discord Guild ID (OPTIONAL - for guild-specific commands)
# If set, commands will be registered only for this guild (faster updates)
# If not set, commands will be registered globally (takes up to 1 hour to update)
# To get your guild ID: Right-click your server → Server Settings → Widget → Server ID
# DISCORD_GUILD_ID=your_guild_id_here

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# API service port (default: 3000)
# Only needed if you want to change the default port
# API_PORT=3000

# API URL for the bot to communicate with the API
# For local Docker development, use: http://api:3000
# For local development without Docker, use: http://localhost:3000
# For Railway production, this will be set automatically via Railway's internal networking
API_URL=http://api:3000

# -----------------------------------------------------------------------------
# Bot Service Configuration
# -----------------------------------------------------------------------------
# Bot health check server port (default: 8080)
# Only needed if you want to change the default port
# BOT_PORT=8080

# -----------------------------------------------------------------------------
# Application Settings (OPTIONAL)
# -----------------------------------------------------------------------------
# Environment: development, production, or test
# Defaults to "production" if not set
NODE_ENV=development

# Logging level: debug, info, warn, error
# Use "debug" for development to see more detailed logs
# Defaults to "info" if not set
LOG_LEVEL=debug

# =============================================================================
# Railway Production Notes
# =============================================================================
# When deploying to Railway, set these variables in the Railway dashboard:
# 
# For API Service:
#   - DATABASE_URL (Railway will provide this automatically if you add a PostgreSQL service)
#   - PORT (Railway sets this automatically)
#   - NODE_ENV=production
#   - LOG_LEVEL=info
#
# For Bot Service:
#   - DISCORD_TOKEN (your production bot token)
#   - DISCORD_APP_ID (your production app ID)
#   - DISCORD_GUILD_ID (optional, for faster command updates)
#   - DATABASE_URL (same as API service, or reference the shared database)
#   - API_URL (use Railway's internal service URL, e.g., http://llama-api.railway.internal:3000)
#   - PORT (Railway sets this automatically)
#   - NODE_ENV=production
#   - LOG_LEVEL=info
#
# =============================================================================
